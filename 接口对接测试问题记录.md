# 前后端接口对接测试问题记录

## 测试范围
- 前台：商品列表/详情、购物车、结算下单、订单列表/详情、支付、取消、评价、售后申请、用户登出
- 后台：管理员登录/登出、统计、用户管理、商品管理、订单管理、售后处理

## 环境信息
- 后端：Flask（http://127.0.0.1:5000）
- 前端：Vite Dev Server（http://127.0.0.1:5173，proxy `/api` -> 5000）

## 问题清单（发现后先记录，再修复）
| ID | 场景 | 复现步骤 | 期望 | 实际 | 根因分析 | 修复方案 | 状态 |
|----|------|----------|------|------|----------|----------|------|
| I-001 | 前端 dev 代理不可用 | 启动 `npm run dev -- --host 127.0.0.1 --port 5173` 后访问 `http://127.0.0.1:5173/api/product/list` | 通过 Vite proxy 转发到后端并返回 200 | 返回 404 | 启动参数未正确透传到 Vite，导致把 `127.0.0.1` 当作 root，配置未生效 | 新增脚本 `npm run dev:local`，固定用 `vite --host 127.0.0.1 --port 5173` 启动 | 已修复 |

## 修复验证清单
- [x] 管理员：bootstrap -> 登录 -> 创建商品 -> 订单状态更新/售后处理
- [x] 用户：注册/登录 -> 加入购物车 -> 结算下单 -> 支付 -> 取消/评价/售后
- [x] 权限：未登录访问受保护接口返回 401；非管理员访问后台返回 403

## 第二轮对接测试产物
- 报告：test_artifacts\\api-integration-round2-20260409-104217.md
- 明细（输入/输出/期望对比）：test_artifacts\\api-integration-round2-20260409-104217.json
